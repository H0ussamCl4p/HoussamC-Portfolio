# Copy this file to `.env.local` (do NOT commit `.env.local`).
# Fill in the values with your secrets.

# GitHub Personal Access Token (server-side only)
# Used by the server route `/api/github-stats` to fetch contribution details via GraphQL.
# DO NOT put real secrets in tracked files. Copy this file to `.env.local` and
# put your real token there instead. If you previously committed a token, see
# the cleanup steps below and rotate the token immediately.
GITHUB_TOKEN=

# Google Drive / Service Account settings
# Option A (recommended for local dev): place your service account JSON file in the repo root
# and set the path below (do NOT commit the JSON file). Example: google-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=./google-service-account.json

# Option B (recommended for CI/CD): store the service account JSON contents as BASE64 in a secret
# then decode it during CI to a file named `google-service-account.json`.
GOOGLE_SERVICE_ACCOUNT_JSON_BASE64=

# The Drive folder id to sync (the folder that contains your designs)
DRIVE_FOLDER_ID=

# Optional: the base URL used when generating absolute image URLs or OG images
NEXT_PUBLIC_BASE_URL=

# Any other server-only secrets you need (add them here)
# EXAMPLE: MATTERMOST_WEBHOOK=, SENTRY_DSN=, etc.
# This file is a placeholder for environment variables required to run this project.
# Currently, there are no environment variables needed.

# Example of what future variables might look like:
# NEXT_PUBLIC_API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
# SECRET_KEY=your_secret_key_here

# Password for protected pages/routes
PAGE_ACCESS_PASSWORD=password
